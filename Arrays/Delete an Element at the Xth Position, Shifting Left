void deleteEl(vector<int> &arr, int pos){
    queue<int> q;
    n = arr.size();

    for(int i=pos+1; i<n; i++){
        q.push(arr[i]);
    }
    while(!q.empty()){
        arr[pos++] = q.front();
        q.pop();
    }
    arr.pop_back();
}
