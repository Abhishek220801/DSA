https://www.geeksforgeeks.org/problems/generate-all-binary-strings/1

void generate(string s, int n){
    if(s.length()==n){
        cout<<s<<endl;
        return;
    }
    generate(s+'0',n);
    if(s.empty() || s[s.length()-1]=='0') generate(s+'1',n);
}
