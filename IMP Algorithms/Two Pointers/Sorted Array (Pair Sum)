bool pairSum(int arr[], int n, int target){
    int i=n-1, j = n-1;
    while(i<j){
        if(arr[i]+arr[j]==target) return true;
        else if(arr[i]+arr[j]<target) i++;
        else j--;
    }
    return false;
}
