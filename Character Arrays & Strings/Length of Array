int getLength(char arr[]){
    int index=0;
    while(arr[index]!='\0'){
        index++;
    }
    return index;
}

//Rest you can also you standard library function strlen to get the length directly
